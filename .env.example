NEXGEN_ENV=development
NEXGEN_RESEND_API_KEY=
NEXGEN_RESEND_FROM_ADDRESS=onboarding@resend.dev
NEXGEN_RESEND_FROM_NAME=NexGen

# Only needed by existing non-password emails that still use PHPMailer/Gmail.
NEXGEN_MAIL_USERNAME=
NEXGEN_MAIL_APP_PASSWORD=
